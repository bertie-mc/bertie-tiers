name: Build and test

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

permissions:
  contents: read

jobs:
  checks:
    uses: bertie-mc/bertie-ci/.github/workflows/neoforge-mod.yml@main
    with:
      sides: both
      gametest: true
      tool-ref: main
